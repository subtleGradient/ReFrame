import { ScrollView, Text } from "react-native"

export default function HomeScreen() {
  return (
    <ScrollView style={{ padding: 16 }}>
      <Text>Streaming text demo</Text>
    </ScrollView>
  )
}
